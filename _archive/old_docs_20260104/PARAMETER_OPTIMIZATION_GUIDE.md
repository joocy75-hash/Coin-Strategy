# 파라미터 최적화 쉬운 가이드

## 🤔 파라미터가 뭔가요?

**전략의 "설정값"**입니다. 게임의 난이도 설정처럼, 숫자를 바꾸면 결과가 달라집니다.

---

## 📊 실제 예시로 이해하기

### 예시 1: Adaptive ML Trailing Stop 전략

현재 설정값 (기본값):
```python
kama_length = 20        # KAMA 계산 기간
fast_length = 15        # 빠른 이동평균
slow_length = 50        # 느린 이동평균
atr_period = 21         # ATR 계산 기간
base_multiplier = 2.5   # 스탑 거리 배수
stop_loss_percent = 5.0 # 고정 손절 비율
```

**이 숫자들을 바꾸면 어떻게 될까요?**

#### 시나리오 A: 보수적 설정 (손실 줄이기)
```python
kama_length = 30        # 20 → 30 (더 안정적)
atr_period = 28         # 21 → 28 (더 넓은 스탑)
base_multiplier = 3.0   # 2.5 → 3.0 (더 넓은 스탑)
stop_loss_percent = 3.0 # 5.0 → 3.0 (더 타이트한 손절)
```
**결과 예상**:
- ✅ 손실이 줄어듦 (MaxDD 감소)
- ❌ 거래 횟수 감소
- ⚖️ 평균 수익률 약간 감소 가능

#### 시나리오 B: 공격적 설정 (수익 극대화)
```python
kama_length = 10        # 20 → 10 (빠른 반응)
atr_period = 14         # 21 → 14 (작은 스탑)
base_multiplier = 2.0   # 2.5 → 2.0 (타이트한 스탑)
stop_loss_percent = 7.0 # 5.0 → 7.0 (여유로운 손절)
```
**결과 예상**:
- ✅ 트렌드에 빠르게 진입
- ❌ 손실도 빠르게 발생 (MaxDD 증가)
- ⚖️ 높은 수익 가능성, 높은 리스크

---

### 예시 2: PMax 전략

현재 설정값:
```python
atr_length = 10           # ATR 계산 기간
upper_multiplier = 1.5    # 숏 포지션 ATR 배수
lower_multiplier = 3.0    # 롱 포지션 ATR 배수
ma_length = 10            # 이동평균 기간
stop_loss_percent = 5.0   # 손절 비율
```

**파라미터 조정 예시**:

#### 최적화 목표 1: 승률 높이기
```python
atr_length = 14           # 10 → 14 (더 안정적인 ATR)
lower_multiplier = 4.0    # 3.0 → 4.0 (더 넓은 롱 스탑)
stop_loss_percent = 4.0   # 5.0 → 4.0 (빠른 손절)
```
**예상 효과**:
- Win Rate 35% → 40% 가능
- 하지만 수익률 감소 가능

#### 최적화 목표 2: Sharpe Ratio 개선
```python
atr_length = 21           # 10 → 21 (매우 안정적)
upper_multiplier = 2.0    # 1.5 → 2.0 (숏 스탑 확대)
lower_multiplier = 2.5    # 3.0 → 2.5 (롱 스탑 축소)
```
**예상 효과**:
- 위험 대비 수익 개선 (Sharpe -0.08 → 0.5+ 목표)
- 거래 빈도 감소

---

## 🔬 파라미터 최적화 방법

### 방법 1: 수동 조정 (초보자 추천)

**단계**:
1. 파라미터 1개만 선택 (예: `kama_length`)
2. 값을 바꿔가며 백테스트 (10, 15, 20, 25, 30)
3. 결과 비교해서 최고 성과 선택
4. 다음 파라미터로 이동

**예시 스크립트**:
```python
# kama_length 최적화
for kama_length in [10, 15, 20, 25, 30]:
    # 백테스트 실행
    stats = bt.run(kama_length=kama_length)
    print(f"kama_length={kama_length}: 수익률={stats['Return [%]']:.2f}%")
```

**결과 예시**:
```
kama_length=10: 수익률=85.23%
kama_length=15: 수익률=92.45%  ← 최고!
kama_length=20: 수익률=100.90% (현재)
kama_length=25: 수익률=78.12%
kama_length=30: 수익률=65.34%
```

→ `kama_length=20`이 최적!

---

### 방법 2: 그리드 서치 (중급자)

**여러 파라미터를 동시에 테스트**:

```python
best_return = -999
best_params = {}

for kama in [10, 20, 30]:
    for atr in [14, 21, 28]:
        for multiplier in [2.0, 2.5, 3.0]:
            # 백테스트
            stats = bt.run(
                kama_length=kama,
                atr_period=atr,
                base_multiplier=multiplier
            )

            if stats['Return [%]'] > best_return:
                best_return = stats['Return [%]']
                best_params = {
                    'kama': kama,
                    'atr': atr,
                    'multiplier': multiplier
                }

print(f"최적 파라미터: {best_params}")
print(f"최고 수익률: {best_return:.2f}%")
```

**주의**: 조합 수가 많아짐 (3×3×3 = 27번 백테스트)

---

### 방법 3: 베이지안 최적화 (고급)

**AI가 자동으로 최적값 찾기**:
- 적은 시도로 최적값 근사
- scikit-optimize 라이브러리 사용

```python
from skopt import gp_minimize

def objective(params):
    kama, atr, multiplier = params
    stats = bt.run(
        kama_length=int(kama),
        atr_period=int(atr),
        base_multiplier=multiplier
    )
    return -stats['Return [%]']  # 음수 (최소화 문제로 변환)

# 탐색 범위
space = [
    (10, 50),    # kama_length
    (10, 30),    # atr_period
    (1.5, 4.0)   # base_multiplier
]

result = gp_minimize(objective, space, n_calls=50)
print(f"최적 파라미터: kama={result.x[0]}, atr={result.x[1]}, mult={result.x[2]}")
```

---

## ⚠️ 과최적화(Overfitting) 주의!

### 나쁜 예시:
```python
# 백테스트에서는 200% 수익!
kama_length = 17.3
atr_period = 23.7
base_multiplier = 2.847
stop_loss_percent = 4.234
```

**문제**: 과거 데이터에만 너무 딱 맞춤 → 실전에서 망함

### 좋은 예시:
```python
# 백테스트에서 120% 수익, 하지만 안정적
kama_length = 20      # 깔끔한 정수
atr_period = 21       # 일반적인 값
base_multiplier = 2.5 # 단순한 값
```

**해결책**:
1. 여러 데이터셋에서 테스트 (이미 10개로 함!)
2. Out-of-sample 테스트 (새로운 기간 데이터)
3. 파라미터를 너무 세밀하게 조정하지 않기

---

## 🎯 현재 전략 최적화 제안

### Adaptive ML Trailing Stop

**현재 성과**: 평균 100.90%, Sharpe 0.40

**최적화 목표**: Sharpe > 1.5, Win Rate > 40%

**추천 파라미터 범위**:
```python
kama_length: [15, 20, 25, 30]        # 현재 20
atr_period: [14, 21, 28]             # 현재 21
base_multiplier: [2.0, 2.5, 3.0]     # 현재 2.5
adaptive_strength: [0.5, 1.0, 1.5]   # 현재 1.0
stop_loss_percent: [3.0, 5.0, 7.0]   # 현재 5.0
```

**예상 개선**:
- 보수적 설정 → Sharpe 0.4 → 0.8+
- 공격적 설정 → 수익률 100% → 150%+

---

### PMax - Asymmetric Multipliers

**현재 성과**: 평균 11.14%, Sharpe -0.08

**최적화 목표**: 수익률 > 50%, Sharpe > 0.5

**추천 파라미터 범위**:
```python
atr_length: [7, 10, 14]              # 현재 10
upper_multiplier: [1.0, 1.5, 2.0]    # 현재 1.5
lower_multiplier: [2.0, 3.0, 4.0]    # 현재 3.0
ma_length: [7, 10, 15]               # 현재 10
```

**예상 개선**:
- 대칭 배수 (2.0×2.0) → 더 균형 잡힌 성과
- 짧은 ATR (7) → 빠른 반응, 높은 수익 가능

---

## 🚀 실전 최적화 스크립트

제가 자동화 스크립트를 만들어드릴까요?

**포함 기능**:
1. ✅ 파라미터 범위 설정
2. ✅ 자동으로 모든 조합 백테스트
3. ✅ 결과를 표와 그래프로 표시
4. ✅ 최적 파라미터 자동 추출
5. ✅ CSV로 저장 (Excel에서 분석 가능)

**예상 소요 시간**:
- 수동 조정: 2시간
- 자동화 스크립트 실행: 30분 (컴퓨터가 알아서 함)

---

## 📊 최적화 결과 예시

최적화 전 vs 후:

| 지표 | 최적화 전 | 최적화 후 | 개선 |
|------|----------|----------|------|
| 평균 수익률 | 100.90% | **135.20%** | +34.3% |
| 샤프 비율 | 0.40 | **1.25** | +212% |
| 승률 | 35.59% | **42.15%** | +6.56% |
| Profit Factor | 1.54 | **2.08** | +35% |
| MaxDD | -47.26% | **-32.14%** | +32% 개선 |

**Moon Dev 기준**: ❌ 미달 → ✅ **2개 항목 통과!**

---

## 💡 요약

1. **파라미터 = 전략의 설정값** (20, 2.5, 5.0 같은 숫자)
2. **최적화 = 최고의 숫자 찾기** (실험을 통해)
3. **방법**: 수동 조정 → 그리드 서치 → AI 자동화
4. **목표**: 수익 증가 + 리스크 감소
5. **주의**: 과최적화 조심 (실전에서 망함)

**자동화 스크립트를 만들어드릴까요?**
- 컴퓨터가 알아서 수백 번 백테스트
- 최적 파라미터 자동으로 찾아줌
- 예쁜 그래프와 표로 결과 표시

원하시면 바로 만들어드리겠습니다! 🚀
