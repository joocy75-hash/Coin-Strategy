# 🎯 TradingView Strategy Research Lab - 핵심 가이드

**최종 업데이트**: 2026-01-04
**프로젝트**: TradingView 전략 자동 수집 및 분석 시스템
**원격 서버**: 5.161.112.248 (Hetzner, Germany)

---

## 📋 목차

1. [프로젝트 개요](#프로젝트-개요)
2. [시스템 아키텍처](#시스템-아키텍처)
3. [배포 워크플로우 (중요!)](#배포-워크플로우-중요)
4. [원격 서버 구성](#원격-서버-구성)
5. [로컬 개발 환경](#로컬-개발-환경)
6. [주요 기능](#주요-기능)
7. [트러블슈팅](#트러블슈팅)
8. [빠른 참조](#빠른-참조)

---

## 프로젝트 개요

### 🎯 목적
TradingView에서 고품질 Pine Script 전략을 자동으로 수집, 분석, 백테스트하여 실전 투자에 활용 가능한 전략을 발굴합니다.

### ✨ 핵심 기능

| 기능 | 설명 | 실행 주기 |
|------|------|-----------|
| **자동 수집** | TradingView에서 500+ 부스트 전략 수집 | 6시간마다 |
| **코드 추출** | Pine Script 소스코드 자동 추출 | 수집 시 |
| **AI 분석** | Claude를 활용한 품질 분석 (리페인팅, 과적합 탐지) | 수집 후 |
| **백테스트** | Binance 데이터로 실전 성능 검증 | 선택적 |
| **리포트 생성** | HTML 형식의 분석 리포트 자동 생성 | 수집 완료 시 |
| **텔레그램 알림** | 수집/분석 결과 실시간 알림 | 이벤트 발생 시 |

### 📊 기술 스택

- **언어**: Python 3.11
- **웹 스크래핑**: Playwright (Chromium)
- **AI 분석**: Anthropic Claude (claude-3-5-sonnet-20241022)
- **백엔드**: FastAPI + Uvicorn
- **데이터베이스**: SQLite (aiosqlite)
- **컨테이너**: Docker + Docker Compose
- **CI/CD**: GitHub Actions
- **알림**: Telegram Bot API

---

## 시스템 아키텍처

### 📐 전체 구조도

```
┌─────────────────────────────────────────────────────────┐
│                     로컬 개발 환경                        │
│  /Users/mr.joo/Desktop/전략연구소/strategy-research-lab │
│                                                           │
│  - 코드 수정 및 테스트                                    │
│  - Git으로 버전 관리                                      │
└──────────────────┬──────────────────────────────────────┘
                   │
                   │ git push origin main
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│                    GitHub Repository                     │
│     https://github.com/joocy75-hash/TradingView-Strategy│
│                                                           │
│  - 소스코드 저장소 (Single Source of Truth)              │
│  - GitHub Actions 트리거                                  │
└──────────────────┬──────────────────────────────────────┘
                   │
                   │ GitHub Actions (자동 배포)
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│              원격 서버 (5.161.112.248)                    │
│         /root/service_c/strategy-research-lab            │
│                                                           │
│  ┌─────────────────┐    ┌──────────────────┐            │
│  │  API Server     │    │  Scheduler       │            │
│  │  (포트 8081)     │    │  (6시간마다)      │            │
│  │                 │    │                  │            │
│  │ - FastAPI       │    │ - 전략 수집       │            │
│  │ - REST API      │    │ - AI 분석        │            │
│  │ - 헬스체크       │    │ - 백테스트       │            │
│  └─────────────────┘    └──────────────────┘            │
│           │                      │                       │
│           └──────────┬───────────┘                       │
│                      ▼                                   │
│           ┌────────────────────┐                         │
│           │   SQLite Database  │                         │
│           │   data/strategies  │                         │
│           └────────────────────┘                         │
└─────────────────────────────────────────────────────────┘
```

### 🔄 데이터 흐름

```
TradingView → Playwright 스크래핑 → Pine Script 추출
    ↓
AI 분석 (Claude) → 품질 점수 산출 → 등급 부여 (A-F)
    ↓
SQLite 저장 → 백테스트 (옵션) → HTML 리포트 생성
    ↓
텔레그램 알림 → FastAPI로 조회 가능
```

---

## 배포 워크플로우 (중요!)

### ⚠️ 절대 원칙: 코드 동기화

**❌ 절대 하지 말 것**:
- 원격 서버에서 직접 코드 수정
- 로컬과 원격 서버의 코드가 달라지는 상황
- GitHub를 거치지 않는 배포

**✅ 반드시 따를 것**:
- 모든 코드 수정은 **로컬에서만** 진행
- 수정 후 **반드시 GitHub에 푸시**
- GitHub Actions가 **자동으로 원격 서버에 배포**

### 📝 표준 배포 절차

#### 1단계: 로컬에서 코드 수정

```bash
# 로컬 환경
cd /Users/mr.joo/Desktop/전략연구소/strategy-research-lab

# 코드 수정 (VS Code, 에디터 등)
# 예: src/config.py, api/server.py 등
```

#### 2단계: Git으로 커밋 및 푸시

```bash
# 변경사항 확인
git status

# 모든 변경사항 스테이징
git add .

# 커밋 (의미 있는 메시지 작성)
git commit -m "Fix: 버그 수정 설명

상세 설명...

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"

# GitHub에 푸시
git push origin main
```

#### 3단계: GitHub Actions 자동 배포 (자동)

푸시하면 자동으로 다음이 실행됩니다:

1. ✅ GitHub Actions 워크플로우 시작
2. ✅ SSH로 원격 서버 접속
3. ✅ 코드 rsync로 전송
4. ✅ .env 파일 생성 (secrets 사용)
5. ✅ Docker 이미지 빌드
6. ✅ 컨테이너 재시작
7. ✅ 헬스체크 (http://5.161.112.248:8081/api/health)

**배포 모니터링**:
```bash
# 워크플로우 상태 확인
gh run list --limit 5

# 실시간 로그 보기
gh run watch
```

#### 4단계: 배포 확인

```bash
# API 헬스체크
curl http://5.161.112.248:8081/api/health

# 컨테이너 상태 확인
ssh root@5.161.112.248 "docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml ps"

# 로그 확인
ssh root@5.161.112.248 "docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml logs --tail=50"
```

### 🚨 긴급 수정이 필요한 경우

원격 서버에서 긴급하게 수정해야 할 경우에도 **즉시 로컬에 반영**:

```bash
# 1. 원격 서버에서 수정한 파일 확인
ssh root@5.161.112.248 "cd /root/service_c/strategy-research-lab && git diff"

# 2. 해당 파일을 로컬로 복사
scp root@5.161.112.248:/root/service_c/strategy-research-lab/src/config.py \
    /Users/mr.joo/Desktop/전략연구소/strategy-research-lab/src/config.py

# 3. 로컬에서 Git 커밋 및 푸시
cd /Users/mr.joo/Desktop/전략연구소/strategy-research-lab
git add src/config.py
git commit -m "Emergency fix: config.py 긴급 수정"
git push origin main
```

### 🔍 동기화 상태 확인

주기적으로 로컬과 원격 서버의 코드가 일치하는지 확인:

```bash
# 로컬 최신 커밋
cd /Users/mr.joo/Desktop/전략연구소/strategy-research-lab
git log -1 --oneline

# 원격 서버에서 확인
ssh root@5.161.112.248 "cd /root/service_c/strategy-research-lab && git log -1 --oneline 2>/dev/null || echo 'Not a git repo'"

# 차이가 있으면 GitHub Actions로 재배포
gh run rerun $(gh run list --limit 1 --json databaseId --jq '.[0].databaseId')
```

---

## 원격 서버 구성

### 🖥️ 서버 정보

| 항목 | 값 |
|------|-----|
| **IP 주소** | 5.161.112.248 |
| **위치** | Hetzner Cloud, Germany |
| **OS** | Ubuntu 22.04 LTS |
| **접속** | `ssh root@5.161.112.248` |
| **프로젝트 경로** | `/root/service_c/strategy-research-lab` |

### 🐳 Docker 컨테이너

#### 1. API Server (strategy-research-lab)

```yaml
컨테이너명: strategy-research-lab
이미지: strategy-research-lab-strategy-lab
포트: 8081:8080
명령: python api/server.py
리소스: CPU 2.0, Memory 2GB
재시작: always
헬스체크: /api/health (30초마다)
```

**주요 엔드포인트**:
- `GET /api/health` - 헬스체크
- `GET /api/stats` - 전략 통계
- `GET /api/strategies` - 전략 목록 (필터, 정렬, 페이징)
- `GET /api/strategy/{id}` - 전략 상세
- `POST /api/backtest` - 백테스트 실행
- `GET /api/docs` - Swagger API 문서

#### 2. Scheduler (strategy-scheduler)

```yaml
컨테이너명: strategy-scheduler
이미지: strategy-research-lab-scheduler
명령: python scripts/auto_collector_service.py
리소스: CPU 1.5, Memory 1.5GB
재시작: always
의존성: strategy-lab (healthy)
```

**실행 로직**:
```python
while True:
    # 1. TradingView 전략 수집 (Playwright)
    # 2. Pine Script 코드 추출
    # 3. DB 저장
    # 4. AI 품질 분석 (Claude)
    # 5. 백테스트 실행 (옵션)
    # 6. HTML 리포트 생성
    # 7. 텔레그램 알림

    sleep(6 hours)  # 6시간 대기
```

### 📁 디렉토리 구조

```
/root/service_c/strategy-research-lab/
├── api/                      # FastAPI 서버
│   └── server.py
├── data/                     # 데이터 저장소 (볼륨 마운트)
│   ├── strategies.db         # SQLite 데이터베이스
│   ├── converted/            # 변환된 Python 전략
│   └── reports/              # HTML 리포트
├── deploy/                   # 배포 관련 파일
│   ├── strategy-collector.service
│   ├── strategy-single.service
│   └── strategy-api.service
├── logs/                     # 로그 파일 (볼륨 마운트)
├── scripts/                  # 유틸리티 스크립트
│   ├── auto_collector_service.py
│   ├── analyze_strategies.py
│   └── generate_beginner_report.py
├── src/                      # 소스 코드
│   ├── analyzer/             # 전략 분석 모듈
│   ├── backtester/           # 백테스트 엔진
│   ├── collector/            # 스크래핑 모듈
│   ├── converter/            # Pine → Python 변환
│   ├── notification/         # 텔레그램 알림
│   ├── storage/              # 데이터베이스 ORM
│   └── config.py             # 설정 관리
├── .env                      # 환경변수 (GitHub Actions가 생성)
├── docker-compose.yml        # Docker Compose 설정
├── Dockerfile                # Docker 이미지 빌드
└── requirements.txt          # Python 의존성
```

### 🔐 환경변수 (.env)

GitHub Actions가 자동으로 생성하는 환경변수:

```env
ANTHROPIC_API_KEY=sk-ant-api03-...  # Claude API 키 (GitHub Secret)
DB_PATH=data/strategies.db
MAX_STRATEGIES=50
MIN_LIKES=500
HEADLESS=true
TIMEOUT=30000
LLM_MODEL=claude-3-5-sonnet-20241022
SKIP_LLM=false
MAX_RETRIES=3
OUTPUT_DIR=data/converted
LOGS_DIR=logs
RATE_LIMIT_DELAY=1.0
```

**텔레그램 알림 추가 (옵션)**:
```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

### 🔧 원격 서버 유지보수 명령어

```bash
# 컨테이너 상태 확인
ssh root@5.161.112.248 "docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml ps"

# 로그 확인
ssh root@5.161.112.248 "docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml logs -f scheduler"

# 컨테이너 재시작
ssh root@5.161.112.248 "docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml restart"

# 컨테이너 중지
ssh root@5.161.112.248 "docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml down"

# 컨테이너 시작
ssh root@5.161.112.248 "docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml up -d"

# 디스크 사용량 확인
ssh root@5.161.112.248 "du -sh /root/service_c/strategy-research-lab/data/*"

# 데이터베이스 백업
ssh root@5.161.112.248 "cp /root/service_c/strategy-research-lab/data/strategies.db /root/service_c/strategy-data-backup-\$(date +%Y%m%d).db"
```

---

## 로컬 개발 환경

### 💻 디렉토리 위치

```
/Users/mr.joo/Desktop/전략연구소/strategy-research-lab/
```

### 🛠️ 개발 환경 설정

#### 1. Python 가상환경 (권장)

```bash
cd /Users/mr.joo/Desktop/전략연구소/strategy-research-lab

# 가상환경 생성
python3.11 -m venv venv

# 활성화
source venv/bin/activate  # macOS/Linux

# 의존성 설치
pip install -r requirements.txt

# Playwright 브라우저 설치
playwright install chromium
```

#### 2. 환경변수 설정

```bash
# .env 파일 생성
cp .env.example .env

# .env 파일 편집
nano .env
```

```env
ANTHROPIC_API_KEY=sk-ant-api03-...
DB_PATH=data/strategies.db
HEADLESS=false  # 로컬에서는 브라우저 보기
```

#### 3. 로컬 실행

```bash
# API 서버 실행
python api/server.py
# → http://localhost:8080/api/docs

# 수집기 실행 (단일 실행)
python scripts/auto_collector_service.py --once

# 수집기 실행 (연속 모드)
python scripts/auto_collector_service.py
```

### 🧪 테스트

```bash
# 데이터베이스 연결 테스트
python scripts/test_db_operations.py

# 분석기 테스트
python scripts/test_new_analyzer.py

# 백테스트 테스트
python -m pytest tests/ -v
```

---

## 주요 기능

### 📥 1. 전략 수집 (Collector)

**담당 모듈**: `src/collector/human_like_scraper.py`

**동작 방식**:
1. Playwright로 TradingView 접속
2. 필터 적용: Strategies, Popular, 500+ boosts
3. 스크롤하며 전략 메타데이터 수집
4. Pine Script 코드 추출 (`src/collector/pine_fetcher.py`)

**수집 기준**:
- 최소 부스트: 500개 이상
- 오픈소스 전략만
- Pine Script 코드 공개된 것

### 🤖 2. AI 품질 분석 (Analyzer)

**담당 모듈**: `scripts/analyze_strategies.py`

**분석 항목**:

| 항목 | 점수 범위 | 설명 |
|------|-----------|------|
| **코드 품질** | 0-100 | 코드 구조, 가독성, 모범 사례 준수 |
| **성능 품질** | 0-100 | 백테스트 결과 (Sharpe, Win Rate) |
| **전반적 품질** | 0-100 | 종합 점수 |
| **리페인팅 점수** | 0-100 | 미래 데이터 참조 여부 (100 = 안전) |
| **과적합 점수** | 0-100 | 과최적화 여부 (100 = 안전) |

**등급 부여**:
- A: 80점 이상 (최고 품질)
- B: 70-79점 (우수)
- C: 60-69점 (양호)
- D: 50-59점 (보통)
- F: 50점 미만 (부적합)

### 🧪 3. 백테스트 (Backtester)

**담당 모듈**: `src/backtester/strategy_tester.py`

**데이터 소스**: Binance (CCXT 라이브러리)

**백테스트 파라미터**:
- 심볼: BTC/USDT
- 타임프레임: 1시간 (1h)
- 기간: 최근 180일
- 초기 자본: 10,000 USDT
- 수수료: 0.03%

**결과 지표**:
- Total Return (%)
- Sharpe Ratio
- Win Rate (%)
- Profit Factor
- Max Drawdown (%)
- Total Trades

### 📊 4. 리포트 생성

**담당 모듈**: `scripts/generate_beginner_report.py`

**출력 파일**: `data/beginner_report.html`

**리포트 내용**:
- 전체 통계 (총 전략, 분석 완료, A/B 등급 수)
- 등급별 전략 목록
- 상위 전략 상세 (코드, 분석 결과, 백테스트)
- 리페인팅/과적합 이슈

### 📱 5. 텔레그램 알림

**담당 모듈**: `src/notification/telegram_bot.py`

**알림 종류**:
- 수집 시작/완료
- 상위 5개 전략 알림
- 백테스트 결과 (수익률 20% 이상)
- 오류 발생
- 서버 상태 (24시간마다)

**설정 방법**:
1. BotFather로 봇 생성
2. 토큰 및 Chat ID 획득
3. .env에 추가:
   ```env
   TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
   TELEGRAM_CHAT_ID=123456789
   ```

---

## 트러블슈팅

### ❌ 문제 1: 스케줄러 컨테이너가 계속 재시작됨

**증상**:
```bash
docker compose ps
# STATUS: Restarting (1) 10 seconds ago
```

**원인**: Python 코드 오류 또는 환경변수 문제

**해결**:
```bash
# 로그 확인
ssh root@5.161.112.248 "docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml logs scheduler --tail=100"

# Pydantic 오류인 경우 → src/config.py 확인
# ImportError인 경우 → requirements.txt 확인 및 재빌드
```

### ❌ 문제 2: API 응답 없음 (504 Gateway Timeout)

**증상**:
```bash
curl http://5.161.112.248:8081/api/health
# (504 Gateway Timeout)
```

**원인**: 컨테이너 미실행 또는 포트 문제

**해결**:
```bash
# 컨테이너 상태 확인
ssh root@5.161.112.248 "docker ps | grep strategy"

# 컨테이너 재시작
ssh root@5.161.112.248 "docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml restart strategy-lab"

# 포트 확인
ssh root@5.161.112.248 "netstat -tlnp | grep 8081"
```

### ❌ 문제 3: GitHub Actions 배포 실패

**증상**:
```bash
gh run list
# ✗ Deploy to Production (failed)
```

**원인**: SSH 키 오류, 빌드 실패, 헬스체크 실패

**해결**:
```bash
# 워크플로우 로그 확인
gh run view

# SSH 키 확인
gh secret list | grep SSH

# 수동 재실행
gh run rerun <run_id>
```

### ❌ 문제 4: 수집기가 0개 전략 수집

**증상**:
```
✅ 수집 완료: 0개 전략
```

**원인**: TradingView 접근 차단, 필터 변경, 네트워크 문제

**해결**:
```bash
# Headless 모드 해제 (로컬에서 테스트)
# .env 파일:
HEADLESS=false

# 로컬에서 실행하여 브라우저 동작 확인
python scripts/auto_collector_service.py --once

# Rate Limit 조정
RATE_LIMIT_DELAY=2.0  # 1초 → 2초로 증가
```

### ❌ 문제 5: 데이터베이스 잠금 (Database is locked)

**증상**:
```
sqlite3.OperationalError: database is locked
```

**원인**: API 서버와 스케줄러가 동시에 DB 접근

**해결**:
```python
# src/storage/database.py에서 이미 해결됨
# aiosqlite + 컨텍스트 매니저 사용

# 만약 발생하면:
# 1. 한쪽 컨테이너 임시 중지
ssh root@5.161.112.248 "docker stop strategy-scheduler"

# 2. DB 작업 완료 후 재시작
ssh root@5.161.112.248 "docker start strategy-scheduler"
```

### ❌ 문제 6: Anthropic API 요금 초과

**증상**:
```
anthropic.RateLimitError: 429 - rate_limit_error
```

**원인**: 너무 많은 전략 분석으로 API 한도 초과

**해결**:
```bash
# 임시로 AI 분석 비활성화
ssh root@5.161.112.248 "cd /root/service_c/strategy-research-lab && \
cat >> .env << 'EOF'
SKIP_LLM=true
EOF"

# 컨테이너 재시작
ssh root@5.161.112.248 "docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml restart scheduler"

# 수집 개수 줄이기
# .env:
MAX_STRATEGIES=20  # 50 → 20
```

---

## 빠른 참조

### 🚀 배포 체크리스트

- [ ] 로컬에서 코드 수정
- [ ] 로컬에서 테스트 실행
- [ ] `git add .` 스테이징
- [ ] `git commit -m "..."` 커밋
- [ ] `git push origin main` 푸시
- [ ] GitHub Actions 성공 확인 (`gh run watch`)
- [ ] API 헬스체크 확인 (`curl http://5.161.112.248:8081/api/health`)
- [ ] 로그 확인 (오류 없는지)

### 📞 주요 명령어 모음

#### 로컬 개발

```bash
# 가상환경 활성화
source venv/bin/activate

# API 서버 실행
python api/server.py

# 수집기 실행 (1회)
python scripts/auto_collector_service.py --once

# 테스트
pytest tests/ -v
```

#### Git 작업

```bash
# 상태 확인
git status

# 변경사항 확인
git diff

# 커밋 및 푸시
git add .
git commit -m "메시지"
git push origin main

# 최근 커밋 확인
git log -1
```

#### 원격 서버

```bash
# SSH 접속
ssh root@5.161.112.248

# 컨테이너 상태
docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml ps

# 로그 확인
docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml logs -f scheduler

# 재시작
docker compose -f /root/service_c/strategy-research-lab/docker-compose.yml restart
```

#### API 테스트

```bash
# 헬스체크
curl http://5.161.112.248:8081/api/health

# 통계
curl http://5.161.112.248:8081/api/stats

# 전략 목록 (A등급만)
curl "http://5.161.112.248:8081/api/strategies?grade=A&limit=10"

# API 문서 (브라우저)
open http://5.161.112.248:8081/api/docs
```

### 📚 관련 문서

| 문서 | 설명 | 경로 |
|------|------|------|
| **핵심.md** | 이 문서 (전체 가이드) | `핵심.md` |
| **DEPLOYMENT_READINESS_REPORT.md** | 배포 준비 상태 보고서 | 프로젝트 루트 |
| **QUICK_DEPLOYMENT_GUIDE.md** | 빠른 배포 가이드 | 프로젝트 루트 |
| **OVERSEAS_IP_POLICY.md** | 해외 IP 안전성 분석 | 프로젝트 루트 |
| **WORK_SESSION_COMPLETE_20260104.md** | 작업 세션 완료 보고서 | 프로젝트 루트 |

### 🔗 주요 링크

- **GitHub 저장소**: https://github.com/joocy75-hash/TradingView-Strategy
- **API 문서**: http://5.161.112.248:8081/api/docs
- **API 헬스**: http://5.161.112.248:8081/api/health
- **원격 서버**: ssh root@5.161.112.248

---

## 📝 변경 이력

| 날짜 | 버전 | 변경 내용 |
|------|------|-----------|
| 2026-01-04 | 1.0 | 초기 문서 작성 |
| 2026-01-04 | 1.1 | Pydantic 2.x 호환성 수정 반영 |
| 2026-01-04 | 1.2 | 배포 워크플로우 지침 강화 |

---

**문서 작성**: Claude Sonnet 4.5
**유지보수**: /Users/mr.joo/Desktop/전략연구소/strategy-research-lab/
**배포 상태**: ✅ 원격 서버 정상 동작 중
