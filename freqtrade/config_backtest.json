{
    "api_server": {
        "enabled": true,
        "listen_ip_address": "0.0.0.0",
        "listen_port": 8082,
        "verbosity": "error",
        "enable_openapi": true,
        "jwt_secret_key": "backtest_secret_key",
        "ws_token": "backtest_token",
        "CORS_origins": ["*"],
        "username": "admin",
        "password": "admin"
    }
}
